DATABASE_URL=postgresql://user:pass@host/db?sslmode=require
ADMIN_KEY=change-me-to-a-long-random-string

# Optional but recommended — higher GitHub API rate limits
# https://github.com/settings/tokens (read public repos)
GITHUB_TOKEN=
